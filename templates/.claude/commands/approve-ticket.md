# Approve Ticket

Use this command when triage has decided that a `needs-triage` ticket should proceed.

## Steps

1. Treat `$ARGUMENTS` as a ticket id, slug, `target/slug`, or absolute ticket path.
2. Run:

```bash
ws approve-ticket soullink "$ARGUMENTS"
```

3. Summarize briefly:
- ticket
- target
- task vs review
